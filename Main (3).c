#include <stdio.h>

int main() {
    int length = 10;
    int width = 7;
    int area;

    area = length * width;

    printf("Length = %d\n", length);
    printf("Width = %d\n", width);
    printf("Area of rectangle = %d\n", area);

    return 0;
}